{
  "scripts": {
    "dev": "node --watch-path=./ --env-file=.env src/index.ts ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "dependencies": {
    "fastify": "^5.7.2"
  },
  "devDependencies": {
    "@types/node": "^25.2.0"
  }
}
